"Smart energy and monitoring system" open with a main home after register it can login. All information
is saved in the files so the state of users could be stored. The function of user have, change budget,
check history record, check billcheck current usage and modify password. At manager frame, manager could view 
user information, search user, add user, remove user, set tariff, check bill which is sent by system. All the 
function could be realized by click button or input to the textarea.
